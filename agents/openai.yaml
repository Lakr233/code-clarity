interface:
  display_name: "Code Clarity"
  short_description: "Reduce state, branches, errors, and seams safely"
  default_prompt: "Use $code-clarity to simplify this change by removing unnecessary state, errors, branches, and abstractions while preserving behavior."

policy:
  allow_implicit_invocation: true
